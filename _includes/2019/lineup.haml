{% for event in site.data.2019.events %}

%div{class: ['event-wrap {{ event.type }}'] }
  - link = "2019/lineup/{{event.searchString}}"
  .avatar.ga-track{onclick: "goToPage('#{link}')"}
    %img{src:'{{event.avatarPath}}', alt:'{{event.title}}', onError:'loadDefaultAvatar(this)'}
  .info-wrap
    .main-info.ga-track{onclick:"goToPage('2019/lineup/{{event.searchString}}')"}
      .title
        {{event.title}}
      .name
        {{event.name}}

{% endfor %}
